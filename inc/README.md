#This Folder should be protected for production site.
In order to protect this folder please rename the ```.htaccess.org``` to ```.htaccess``` 
or
just create a new ```.htaccess``` and add 

```Deny from all```
